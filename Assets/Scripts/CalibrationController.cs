using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class CalibrationController : MonoBehaviour {


	// we want to simulate the playing, so that we can place it in the center of the screen and then user can adjust it with the global offset


	public GameObject notePrefab;
	// Use this for initialization
	void Start () {
	//	Note note = new Note (1.0f, 0.0f);
	//	GameObject noteGO = Instantiate(notePrefab, Camera.main.transform.position, Quaternion.identity) as GameObject;
	//	NoteController noteC = noteGO.GetComponent<NoteController>();
	//	noteC.SetNote(note);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
